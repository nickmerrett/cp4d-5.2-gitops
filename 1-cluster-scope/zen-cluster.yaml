apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: zen-cluster-${appSuffix}
  namespace: ${argocdNS}
spec:
  destination:
    name: ''
    namespace: default
    server: https://kubernetes.default.svc
  source:
    path: ''
    repoURL: ${helmRepoURL}
    targetRevision: ${zen-cluster-version}
    chart: ${zen-cluster-chart}
    helm:
      valuesObject:
        global:
          operatorNamespace: ${operatorNS}
  project: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
