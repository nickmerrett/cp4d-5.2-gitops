apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: rh-cert-manager-gitops
  namespace: openshift-gitops
spec:
  destination:
    name: ''
    namespace: default
    server: https://kubernetes.default.svc
  source:
    repoURL: https://github.com/gitops-cp4d/non-olm-cp4d-gitops.git
    targetRevision: main
    path: repos
    chart: rh-cert-manager
    helm:
      releaseName: cert-manager-operator-helm
      valueFiles:
        - ../values.yaml
  project: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
