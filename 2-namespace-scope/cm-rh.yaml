apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: rh-cert-manager-${appSuffix}
  namespace: ${argocdNS}
spec:
  destination:
    name: ''
    namespace: default
    server: https://kubernetes.default.svc
  source:
    repoURL: ${helmRepoURLdev}
    targetRevision: main
    path: repos
    chart: rh-cert-manager
    helm:
      releaseName: cert-manager-operator-helm
      valueFiles:
        - ../values.yaml
  project: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
