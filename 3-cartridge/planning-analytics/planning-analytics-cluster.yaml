apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: planning-analytics-cluster-${appSuffix}
  namespace: ${argocdNS}
spec:
  destination:
    name: ''
    namespace: default
    server: https://kubernetes.default.svc
  source:
    path: ''
    repoURL: ${helmRepoURL}
    targetRevision: ${planning-analytics-cluster-version}
    chart: ${planning-analytics-cluster-chart}
    helm:
      valuesObject:
        global:
          operatorNamespace: ${operatorNS}
  project: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
