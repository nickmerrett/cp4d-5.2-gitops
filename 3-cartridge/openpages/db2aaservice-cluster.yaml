apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: db2aaservice-cluster-${appSuffix}
  namespace: ${argocdNS}
  labels:
    scope: cluster
    product: openpages
spec:
  destination:
    name: ''
    namespace: default
    server: https://kubernetes.default.svc
  source:
    repoURL: ${argoSourceRepoURL}
    targetRevision: main
    path: repos/db2aaservice/cluster-scope
  project: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true